# Gitconfig
#
# You probably want to change the [user] section here since you are not me.

[user]
        name = Philip Gardner
        email = philip.gardner@gmail.com
[credential]
        helper = osxkeychain
[color]
        diff = auto
        status = auto
        branch = auto
        ui = true
